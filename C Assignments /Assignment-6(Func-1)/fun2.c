//to find no is odd or even
#include<stdio.h>
int evenodd(int)
void main()
{
	int no;
	printf("Enter the number");
	scanf("%d",&no);
	int res=oddeven(no);
	if(res==1)
		printf("even");
	else
		printf("odd")
}
int oddeven(int n)
{
	while(no>0)
	{
	if(no%2==0)
		return 1;
	}
	return 0
}