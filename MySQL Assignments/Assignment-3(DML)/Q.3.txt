Q.3 Write a SQL statement to insert one row in jobs table to ensure that no duplicate
value will be entered in the job_id column.

INSERT INTO Jobs (job_id, job_title, min_salary, max_salary)
VALUES (201, 'Data Analyst', 5000, 15000);
